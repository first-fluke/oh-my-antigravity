---
title: Рабочие процессы
description: Полный справочник по всем 16 рабочим процессам oh-my-agent — слеш-команды, постоянный и непостоянный режимы, ключевые слова-триггеры на 11 языках, фазы и шаги, читаемые и записываемые файлы, механика автодетекции через triggers.json и keyword-detector.ts, фильтрация информационных паттернов и управление состоянием постоянного режима.
---

# Рабочие процессы

Рабочие процессы — это структурированные многоэтапные процессы, запускаемые слеш-командами или ключевыми словами в естественном языке. Они определяют, как агенты совместно работают над задачами — от однофазных утилит до сложных 5-фазных шлюзов качества.

Всего 16 рабочих процессов, 4 из которых — постоянные (сохраняют состояние и не могут быть случайно прерваны).

---

## Выбор навыка или рабочего процесса {#choosing-a-skill-or-workflow}

Выбирайте по тому, какая координация и проверка нужны задаче. Если рабочий процесс уже выбран, следуйте ему; продолжайте активный процесс, пока явно не отмените или не смените его. Для новой задачи без выбранного процесса используйте эту таблицу:

| Что нужно задаче | Что выбрать | Пример |
|---|---|---|
| Один домен без координации агентов | [Один навык](/docs/guide/single-skill) | Добавить API-эндпоинт и протестировать валидацию |
| Несколько доменов с пошаговым планированием, реализацией и QA | `/work` | Согласовать изменение API с веб-клиентом и мобильным клиентом |
| Автоматическое делегирование независимых задач для параллельного выполнения | `/orchestrate` | После разрешения зависимостей параллельно реализовать задачи бэкенда и фронтенда |
| Полный процесс обеспечения качества по явному запросу | `/ultrawork` | Пройти планирование, реализацию, проверку, доработку и ревью готовности к релизу |
| Явный запрос повторять работу до выполнения критериев, проверяемых программно | `/ralph` | Повторять реализацию и независимую проверку до прохождения указанных регрессионных проверок в пределах ограничений цикла |

Перед запуском агентов `/orchestrate` загружает пригодный план или создаёт его через `/plan`. Запускать `/plan` заранее не требуется. Поэтому наличие плана не определяет выбор между `/work` и `/orchestrate`: выбирайте способ координации. Оба процесса могут выполнять независимые задачи параллельно.

Критерии приёмки и тесты нужны и при работе с одним навыком. Сами по себе они не требуют `/ralph`: каждая итерация Ralph включает полный процесс ultrawork и независимого judge. Выбирайте его, когда нужен именно такой повторяющийся цикл проверки. При срабатывании предохранителей цикл может остановиться с незавершённой или заблокированной работой.

Таблица даёт рекомендации по выбору, а не задаёт автоматическую маршрутизацию рабочих процессов. Основной агент может предложить подходящий способ работы; рекомендация или объяснение процесса не запускает его. Слеш-команда явно выбирает процесс. Если включён хук определения ключевых слов, совпадение с настроенными словами или шаблонами также может активировать процесс с учётом фильтров информационных запросов. Детектор не определяет число доменов, не проверяет готовность плана и не использует таблицу как алгоритм приоритетов.

При ревью плана действует уже полученное разрешение на задачу. Агенты спрашивают только при отсутствии существенного решения или для действия за пределами согласованного объёма. Ревью готовности к релизу само по себе не разрешает публикацию или деплой.

---

## Постоянные рабочие процессы

Постоянные рабочие процессы работают до завершения всех задач. Они сохраняют состояние в `.agents/state/` и повторно внедряют контекст `[OMA PERSISTENT MODE: ...]` при каждом сообщении пользователя до явной деактивации.

### /orchestrate

**Описание:** Автоматизированное параллельное выполнение агентов через CLI. Запускает субагентов, координирует через MCP-память, отслеживает прогресс и запускает циклы верификации.

**Постоянный:** Да. Файл состояния: `.agents/state/orchestrate-state.json`.

**Ключевые слова-триггеры:**
| Язык | Ключевые слова |
|------|---------------|
| Универсальные | "orchestrate" |
| Английский | "parallel", "do everything", "run everything" |
| Корейский | "자동 실행", "병렬 실행", "전부 실행", "전부 해" |
| Японский | "オーケストレート", "並列実行", "自動実行" |
| Китайский | "编排", "并行执行", "自动执行" |
| Испанский | "orquestar", "paralelo", "ejecutar todo" |
| Французский | "orchestrer", "parallèle", "tout exécuter" |
| Немецкий | "orchestrieren", "parallel", "alles ausführen" |
| Португальский | "orquestrar", "paralelo", "executar tudo" |
| Русский | "оркестровать", "параллельно", "выполнить всё" |
| Нидерландский | "orkestreren", "parallel", "alles uitvoeren" |
| Польский | "orkiestrować", "równolegle", "wykonaj wszystko" |

**Регулярные выражения-триггеры** (намерение + белый список существительных, см. [Автодетекция: Поле Pattern](#pattern-field-raw-regex)):
| Секция | Шаблон | Примеры срабатываний |
|--------|--------|---------------------|
| `*` (универсальный) | `(build\|create\|make\|develop\|implement\|scaffold) + (a\|an\|the) + [modifier]{0,3} + <noun>` | «Build a TODO app with user authentication», «Create an awesome web service», «Develop a backend with PostgreSQL» |
| `*` (универсальный) | `i want a/an + <noun>` | «I want a CLI for parsing logs» |
| `ko` | `<noun> + (을\|를\|이\|가)? + (만들어\|구현해\|개발해 + 변형)` | «TODO 앱 만들어줘», «REST API 구현해», «백엔드를 개발해주세요» |

Белый список существительных (15): app, api, service, server, cli, tool, website, dashboard, system, feature, backend, frontend, prototype, mvp, bot.

**Шаги:**
1. **Шаг 0 — Подготовка:** Чтение навыка координации, руководства по загрузке контекста, протокола памяти. Определение вендора.
2. **Шаг 1: Загрузка/Создание плана:** Проверить `.agents/results/plan-{sessionId}.json`, затем последний `plan-*.json`. Если плана нет или он не готов к выполнению (у задачи не указаны агент, уровень приоритета, зависимости или критерии приёмки), создать его через `/plan` в текущем процессе, сохранив ID сессии. Представить план и использовать уже полученное разрешение; перед делегированием спрашивать только при отсутствии существенного решения или необходимости нового разрешения.
3. **Шаг 2: Инициализация сессии:** Загрузить `oma-config.yaml`, показать таблицу соответствия CLI, использовать ID сессии из этапа создания плана или сгенерировать новый (`session-YYYYMMDD-HHMMSS`), создать `orchestrator-session.md` и `task-board.md` в памяти.
4. **Шаг 3 — Запуск агентов:** Для каждого приоритетного уровня (P0 первый) запуск агентов соответствующим вендору методом. Не превышать MAX_PARALLEL.
5. **Шаг 4 — Мониторинг:** Опрос файлов `progress-{agent}.md`, обновление `task-board.md`.
6. **Шаг 5 — Верификация:** Запуск `verify.sh` для каждого завершённого агента. При сбое — повторный запуск (макс. 2 попытки). После 2 попыток — цикл исследования.
7. **Шаг 6 — Сбор:** Чтение всех файлов `result-{agent}.md`, составление сводного отчёта.
8. **Шаг 7 — Финальный отчёт:** Резюме сессии с Quality Score и Experiment Ledger при наличии.

**Когда использовать:** Большие проекты, требующие максимального параллелизма.

---

### /work

**Описание:** Пошаговая координация нескольких доменов. Сначала PM составляет план, затем агенты выполняют работу в согласованном объёме, после чего следуют QA-ревью и устранение замечаний.

**Постоянный:** Да. Файл состояния: `.agents/state/work-state.json`.

**Ключевые слова-триггеры:**
| Язык | Ключевые слова |
|------|---------------|
| Универсальные | "work", "step by step" |
| Корейский | "코디네이트", "단계별" |
| Японский | "コーディネート", "ステップバイステップ" |
| Китайский | "协调", "逐步" |
| Испанский | "coordinar", "paso a paso" |
| Французский | "coordonner", "étape par étape" |
| Немецкий | "koordinieren", "schritt für schritt" |

**Шаги:**
1. **Шаг 0 — Подготовка:** Чтение навыков, загрузки контекста, протокола памяти.
2. **Шаг 1 — Анализ требований:** Определение задействованных доменов.
3. **Шаг 2 — Планирование PM-агентом:** Декомпозиция требований, API-контракты, сохранение в `.agents/results/plan-{sessionId}.json`.
4. **Шаг 3: Ревью плана:** Представить план и продолжить в пределах уже полученного разрешения. Спрашивать только при отсутствии существенного решения или необходимости нового разрешения.
5. **Шаг 4 — Запуск агентов:** По приоритетным уровням, параллельно в пределах уровня.
6. **Шаг 5 — Мониторинг:** Верификация соответствия API-контрактов.
7. **Шаг 6 — QA-ревью:** Безопасность (OWASP), производительность, доступность, качество кода.
8. **Шаг 6.1 — Quality Score** (условный).
9. **Шаг 7 — Итерация:** При CRITICAL/HIGH — повторный запуск. При повторении — цикл исследования.

**Когда использовать:** Фичи, охватывающие несколько доменов и требующие пошаговой координации планирования, реализации и QA.

---

### /ultrawork

**Описание:** Рабочий процесс максимального качества. 5 фаз, 17 шагов, 11 шагов ревью. Каждая фаза имеет шлюз.

**Постоянный:** Да. Файл состояния: `.agents/state/ultrawork-state.json`.

**Ключевые слова-триггеры:** "ultrawork", "ulw" (универсальные).

**Фазы и шаги:**

| Фаза | Шаги | Агент | Перспектива ревью |
|------|------|-------|------------------|
| **PLAN** | 1-4 | PM-агент (инлайн) | Полнота, Мета-ревью, Избыточность/Простота |
| **IMPL** | 5 | Dev-агенты (запущенные) | Реализация |
| **VERIFY** | 6-8 | QA-агент (запущенный) | Соответствие, Безопасность (OWASP), Регрессии |
| **REFINE** | 9-13 | Debug-агент (запущенный) | Разбиение файлов, Переиспользование, Каскад, Консистентность, Мёртвый код |
| **SHIP** | 14-17 | QA-агент (запущенный) | Качество кода, UX-поток, Связанные проблемы, Готовность к деплою |

**Определения шлюзов:**
- **PLAN_GATE:** План задокументирован, допущения перечислены, альтернативы рассмотрены, проверка на избыточность выполнена, объём работы согласован.
- **IMPL_GATE:** Применимые проверки без генерации артефактов и тесты пройдены, изменены только запланированные файлы, базовый Quality Score записан (если измерялся). Проверки сборки выполняются только по явному запросу.
- **VERIFY_GATE:** Реализация соответствует требованиям, ноль CRITICAL, ноль HIGH, Quality Score >= 75.
- **REFINE_GATE:** Нет больших файлов/функций, побочные эффекты проверены, код очищен, Quality Score не регрессировал.
- **SHIP_GATE:** Проверки качества пройдены, UX проверен, связанные проблемы решены, чек-лист деплоя выполнен, финальный Quality Score >= 75 и не снизился относительно базового значения (если измерялся). Действует уже полученное разрешение; публикация или деплой требуют разрешения на соответствующее действие.

**Поведение при провале:** Первый — вернуться и исправить. Второй — цикл исследования (2-3 гипотезы).

**Условие пропуска REFINE:** Простые задачи до 50 строк.

**Когда использовать:** Production-critical код с всесторонним ревью.

---

### /ralph

**Описание:** Постоянный самоссылающийся цикл выполнения. Оборачивает ultrawork независимым верификатором, который после каждой итерации проверяет критерии завершения. Если все критерии выполнены, сообщает о полном завершении; если остались только выполненные и заблокированные, о частичном завершении; при срабатывании предохранителей останавливается.

**Постоянный:** Да. Файл состояния: `.agents/state/ralph-state.json`.

**Ключевые слова-триггеры:**
| Язык | Ключевые слова |
|------|----------------|
| Универсальные | "ralph" |
| Английский | "don't stop", "until done", "keep going", "finish everything", "run to completion" |
| Корейский | "랄프", "멈추지마", "끝까지", "완료될때까지", "끝장내" |
| Японский | "止まるな", "完了まで", "最後まで", "全部終わらせて" |
| Китайский | "不要停", "直到完成", "全部完成", "做完为止" |
| Испанский | "no pares", "hasta completar", "termina todo" |
| Французский | "n'arrête pas", "jusqu'à complétion", "termine tout" |
| Немецкий | "hör nicht auf", "bis zur fertigstellung", "alles fertigstellen" |

**Фазы:**
1. **Фаза 0: INIT:** Загрузить необходимые материалы (context-loading, протокол памяти, протокол судьи). Определить и записать программно проверяемые критерии завершения: проверки в тестах, проверку типов без генерации артефактов, коды выхода или наличие файлов. Включать проверки сборки только по явному запросу. Показать критерии и продолжить работу в согласованном объёме. Инициализировать сессию с `max_iterations: 5`.
2. **Фаза 1 — WORK:** Выполнить ultrawork (PLAN → IMPL → VERIFY → REFINE → SHIP) как одну итерацию.
3. **Фаза 2: JUDGE:** Независимый верификатор сверяет каждый критерий с фактическим состоянием проекта: выполняет разрешённые проверки и проверяет наличие файлов. Записывает подтверждающие результаты и статус критерия: PASS, FAIL, REGRESSED или BLOCKED.
4. **Фаза 3: DECIDE:** Если все критерии PASS → сообщить о полном завершении. Если остались только PASS и BLOCKED → сообщить о частичном завершении. Если есть FAIL или REGRESSED → передать контекст неудачи в следующую итерацию с учётом предохранителей.
5. **Предохранители:** Цикл останавливается, если `current_iteration >= max_iterations` (по умолчанию 5), или если один и тот же критерий проваливается 3 раза подряд по одной и той же коренной причине (обнаружение застревания).

**Ключевое отличие от /ultrawork:** Ultrawork выполняет 5-фазный процесс с повторными попытками при провале шлюза фазы. Ralph оборачивает ultrawork в цикл повторных попыток с независимым judge, который объективно проверяет завершение. Цикл заканчивается отчётом о полном завершении, о частичном завершении из-за заблокированной работы или о срабатывании предохранителей.

**Читаемые файлы:** `.agents/workflows/ralph/resources/judge-protocol.md`, все файлы ultrawork.
**Записываемые файлы:** `session-ralph.md` (память), логи итераций, итоговый отчёт.

**Когда использовать:** Когда явно требуется повторять выполнение и независимую проверку по программно проверяемым критериям завершения. Сами по себе тесты не требуют Ralph; учитывайте полный процесс ultrawork в каждой итерации и предохранители цикла.

---

## Непостоянные рабочие процессы

### /plan

**Описание:** PM-управляемая разбивка задач с API-контрактами.

**Триггеры:** "task breakdown" (универсальный), "plan" (английский), "계획" (корейский), "計画" (японский), "计划" (китайский).

**Шаги:** Сбор требований -> Анализ целесообразности -> Определение API-контрактов -> Декомпозиция -> Ревью с пользователем -> Сохранение. Вывод: `.agents/results/plan-{sessionId}.json`. Выполнение: инлайн.

---

### /exec-plan

**Описание:** Управление планами выполнения как артефактами репозитория в `docs/exec-plans/`.

**Триггеры:** Нет (только явный вызов).

**Шаги:** Подготовка -> Анализ объёма (оценка сложности: Simple/Medium/Complex) -> Создание плана выполнения (markdown в `docs/exec-plans/active/`) -> Определение API-контрактов (если кросс-доменное взаимодействие) -> Ревью с пользователем -> Выполнение (передача в `/orchestrate` или `/work`) -> Завершение (перемещение в `completed/`).

**Вывод:** `docs/exec-plans/active/{plan-name}.md` с таблицей задач, журналом решений, заметками о прогрессе.

**Когда использовать:** После `/plan` для сложных фич, требующих отслеживаемого выполнения с журналом решений.

---

### /brainstorm

**Описание:** Идеация «сначала дизайн» с 2-3 подходами и документом проектирования.

**Триггеры:** "brainstorm" (универсальный), "ideate" (английский), "브레인스토밍" (корейский), "ブレインストーミング" (японский), "头脑风暴" (китайский).

**Правила:** Без реализации до утверждения дизайна. Без кода. YAGNI.

---

### /architecture

**Описание:** Рабочий процесс архитектуры ПО — диагностика проблем архитектуры, выбор подходящего метода анализа (диагностическая маршрутизация / design-twice / ATAM / CBAM / ADR), сравнение вариантов, синтез мнений заинтересованных сторон и выработка рекомендации, ревью или ADR.

**Триггеры:** "architecture", "ADR", "ATAM", "CBAM" (универсальные), "architecture review", "architectural tradeoff" (английский), "아키텍처", "설계 검토" (корейский), "アーキテクチャ" (японский), "架构" (китайский).

**Шаги:** Обрамление решения (новая архитектура / ревью / анализ компромиссов / приоритизация инвестиций / составление ADR) -> Выбор методологии через диагностическую маршрутизацию -> Анализ текущей архитектуры через MCP-анализ кода (`get_symbols_overview`, `find_symbol`, `find_referencing_symbols`) -> Синтез мнений заинтересованных сторон (только когда решение достаточно сквозное, чтобы оправдать затраты) -> Выработка рекомендации с явными допущениями, компромиссами, рисками и шагами валидации -> Передача в `/plan`, когда требуется реализация.

**Правила:** НЕ писать код реализации или планы задач в этом рабочем процессе. Передавать в `/plan` после принятия архитектурного решения. Использовать MCP-инструменты на всём протяжении; не заменять сырыми чтениями файлов или grep.

**Когда использовать:** Выбор архитектуры системы, решения о границах модулей/сервисов/владения, приоритизация рефакторинга, составление ADR, исследование архитектурной боли (усиление изменений, скрытые зависимости, неудобные API).

---

### /deepinit

**Описание:** Полная инициализация проекта. Генерация AGENTS.md, ARCHITECTURE.md и базы знаний `docs/`.

**Триггеры:** "deepinit" (универсальный), "프로젝트 초기화" (корейский), "プロジェクト初期化" (японский), "项目初始化" (китайский).

---

### /review

**Описание:** Полный пайплайн QA-ревью: OWASP Top 10, производительность, WCAG 2.1 AA, качество кода.

**Триггеры:** "code review", "security audit" (универсальные), "review" (английский), "리뷰" (корейский), "レビュー" (японский), "审查" (китайский).

**Опциональный цикл исправления** с `--fix`: до 3 итераций.

**Делегирование:** При большом объёме шаги 2-7 делегируются заспавненному QA-субагенту.

---

### /deepsec

**Описание:** Управляет скиллом `oma-deepsec` от начала до конца. Устанавливает `.deepsec/`, калибрует стоимость, выполняет проходы scan/process/triage/revalidate/export, гейтит PR через `process --diff`, пишет пользовательские matcher'ы и маршрутизирует находки специализированным агентам. Inline-выполнение (без спавна субагентов).

**Триггеры:** "/deepsec", "deepsec workflow" (универсальные), "run deepsec", "deepsec scan this repo", "deepsec pr review", "deepsec ci gate", "deepsec triage", "deepsec matchers" (английский).

**Шаги:**
1. **Шаг 1, Загрузка скилла:** Прочитайте `.agents/skills/oma-deepsec/SKILL.md`, затем загрузите только ресурсные файлы, соответствующие распознанному интенту (`setup.md`, `scanning.md`, `pr-review.md`, `matchers.md`, `triage.md`, `config.md`). Если `.deepsec/` уже существует в корне репозитория, обрабатывайте запуск как инкрементальный и никогда не выполняйте `init` повторно.
2. **Шаг 2, Классификация интента:** Распознать как ровно один из `setup`, `scan`, `pr-review`, `matchers`, `triage`, `config`, `troubleshoot`. Multi-intent промпты выполняются последовательно. Вставьте `setup` перед любым AI-интентом, если `.deepsec/` отсутствует.
3. **Шаг 3, Подтверждение агента:** Перед любым платным вызовом подтвердите `claude` (сильнейшее рассуждение, самый дорогой) vs `codex` (read-only песочница, дешевле). Пропустите, если пользователь назвал одного, `deepsec.config.ts` фиксирует `defaultAgent` или выбор делегирован.
4. **Шаг 4, Выполнение интента:** `setup` - `bunx deepsec init` + создание `data/<id>/INFO.md` (требуется подтверждение пользователя). `scan` - калибровка `--limit 50 --concurrency 5`, экстраполяция стоимости, полный `process`, `triage --severity HIGH` + `revalidate --min-severity HIGH`, `export`. `pr-review` - `process --diff origin/${BASE_REF} --comment-out comment.md` в two-job CI-паттерне. `matchers` - запись `.deepsec/matchers/<slug>.ts` на нужном уровне шума (`precise`/`normal`/`noisy`) и проверка `scan --matchers`. `triage` - фильтр экспорта до `true-positive`/`uncertain`. `config`/`troubleshoot` - таблица симптомов из `resources/config.md`.
5. **Шаг 5, Резюме и маршрутизация:** Сформируйте сводку запуска (project id, тип прохода, agent/model, отсканированные файлы, находки, TP после revalidate, стоимость, wall time, условия остановки). Маршрутизируйте по **слою уязвимого файла** (backend -> `oma-backend`, frontend -> `oma-frontend`, mobile -> `oma-mobile`, IaC -> `oma-tf-infra`, DB -> `oma-db`, CI -> `oma-dev-workflow`, документ. дрейф -> `oma-docs`, пробел entry-point -> возврат к 4D). При неоднозначном слое или `revalidation.verdict === "uncertain"` сначала `oma-debug` как шаг триажа.
6. **Шаг 6, Условия остановки:** Завершение по завершённому интенту + сводка шага 5, блокирующее предусловие (отсутствие credential, отказ от `INFO.md`) или остановка по квоте с безопасной командой возобновления.

**Правила:** Не модифицировать продуктовый код в этом workflow (передавать специалистам). Не выводить и не коммитить credentials (`vck_…`, `sk-ant-…`, OIDC-токены). Не выдавать `pull-requests: write` CI-джобам, исполняющим код из PR. Возобновлять, а не сбрасывать: при прерывании перезапускайте ту же команду; никогда `rm -rf data/<id>/` без явной инструкции пользователя.

**Когда использовать:** Agent-powered сканирование уязвимостей репозитория, CI/PR security-гейтинг через `process --diff`, написание project-specific matcher'ов для покрытия entry-points, триаж существующих находок для сокращения FP.

---

### /debug

**Описание:** Структурированная диагностика: воспроизведение, корневая причина, минимальное исправление, регрессионный тест, сканирование похожих паттернов.

**Триггеры:** "debug" (универсальный), "fix bug" (английский), "디버그" (корейский), "デバッグ" (японский), "调试" (китайский).

**Критерии запуска субагента:** Ошибка в нескольких доменах, объём > 10 файлов, глубокая трассировка.

---

### /design

**Описание:** 7-фазный рабочий процесс: SETUP -> EXTRACT -> ENHANCE -> PROPOSE -> GENERATE -> AUDIT -> HANDOFF.

**Триггеры:** "design system", "DESIGN.md" (универсальные), "design", "landing page" (английский), "디자인" (корейский), "デザイン" (японский), "设计" (китайский).

**Обязательно:** Весь вывод responsive-first (мобильные 320-639px, планшет 768px+, десктоп 1024px+).

---

### /scm

**Описание:** Conventional Commits с автоматическим разбиением по фичам.

**Триггеры:** Нет (только явный вызов).

**Правила:** Никогда `git add -A`. Никогда не коммитить секреты. HEREDOC для многострочных. Co-Author: `First Fluke <our.first.fluke@gmail.com>`.

---

### /tools

**Описание:** Управление видимостью MCP-инструментов.

**Триггеры:** Нет (только явный вызов).

**Группы:** memory, code-analysis, code-edit, file-ops.

---

### /convert

**Описание:** Конвертация файла из одного формата в другой с маршрутизацией по категории медиа. **Документы** (PDF через `opendataloader-pdf`/`oma-pdf`; HWP/HWPX/HWPML через `kordoc`/`oma-hwp`) извлекаются в Markdown. **Изображения**, **видео** и **аудио** перекодируются в целевой формат через `ffmpeg` (уже подготовлен для `oma-video`).

**Триггеры:** Нет (вызывается явно с путём к входному файлу).

**Шаги:** Валидация ввода и маршрутизация по категории (документ `.pdf`/`.hwp*`; изображение `.jpg`/`.png`/`.webp`/…; видео `.mp4`/`.mov`/…; аудио `.mp3`/`.wav`/…) -> Определение целевого формата (по умолчанию для документов — Markdown; для медиа — явный `--to`) -> Конвертация (PDF: `uvx opendataloader-pdf`, отсканированные PDF используют гибридный режим с OCR; HWP: `bunx kordoc@latest`; медиа: `ffmpeg`) -> Нормализация документов (PDF: `uvx mdformat`; HWP: `flatten-tables.ts`) -> Проверка (чтение Markdown / `ffprobe` для медиа) -> Отчёт об исходном→целевом формате и о любых решениях по качеству/кодекам.

**Правила:** Маршрутизируйте по категории — никогда не запускайте конвертер документов на медиафайле и наоборот. По умолчанию место вывода — тот же каталог, что и у входного файла. Сообщайте о решениях по качеству/кодекам для медиа (перекодирование не является lossless). Никогда не пропускайте шаги. Язык ответа соответствует `.agents/oma-config.yaml`.

**Когда использовать:** Конвертация PDF или корейских документов семейства HWP в Markdown для контекста LLM или приёма в RAG, либо перекодирование изображений (jpg→webp/png), видео (mov→mp4, mp4→gif) и аудио (wav→mp3) между форматами.

---

### /stack-set

**Описание:** Автоопределение стека и генерация языко-специфичных справочников.

**Триггеры:** Нет (только явный вызов).

**Вывод:** Файлы в `.agents/skills/oma-backend/stack/`.

---

## Навыки vs. Рабочие процессы

| Аспект | Навыки | Рабочие процессы |
|--------|--------|-----------------|
| **Что это** | Экспертиза агента | Оркестрированные процессы |
| **Расположение** | `.agents/skills/oma-{name}/` | `.agents/workflows/{name}.md` |
| **Активация** | Автоматическая через ключевые слова | Слеш-команды или триггеры |
| **Область** | Один домен | Многоэтапный, часто мультиагентный |
| **Примеры** | «Создать React-компонент» | «Спланировать -> реализовать -> ревью -> коммит» |

---

## Автодетекция: Как это работает

### Система хуков

oh-my-agent использует хук `UserPromptSubmit`, состоящий из:

1. **`triggers.json`**: Маппинг ключевых слов к рабочим процессам для 11 языков.
2. **`keyword-detector.ts`**: Сканирует ввод пользователя и внедряет контекст активации.
3. **`persistent-mode.ts`**: Проверяет активные файлы состояния и подкрепляет постоянный режим.

### Поток обнаружения

1. Пользователь вводит текст на естественном языке
2. Хук проверяет наличие явной `/command` (если она есть — детекция пропускается во избежание дублирования)
3. Хук санитизирует ввод (удаляет блоки кода, кавычные строки, вставленные блоки системных эхо), затем сканирует его против `.agents/hooks/core/triggers.json` — как по спискам ключевых слов (литеральные фразы), так и по `patterns` (сырые regex). Защита от повторов подавляет повторное срабатывание, если тот же рабочий процесс был запущен 2 и более раз за последние 60 секунд.
4. Если совпадение найдено, проверяется соответствие ввода информационным паттернам
5. Если ввод информационный (например, «what is orchestrate?»), он отфильтровывается — рабочий процесс не запускается
6. Если ввод действенный, в контекст внедряется `[OMA WORKFLOW: {workflow-name}]`
7. Агент читает внедрённый тег и загружает соответствующий файл рабочего процесса из `.agents/workflows/`

### Соглашение о языковых секциях

`.agents/hooks/core/triggers.json` использует структуру с разделением по языкам для `keywords`, `patterns` и `informationalPatterns`:

| Секция | Поведение |
|--------|-----------|
| `*` | Универсальная — загружается всегда, независимо от настройки `language` в `.agents/oma-config.yaml`. Используйте для английского содержимого (lingua franca) и действительно кросс-языковых токенов (например, имя рабочего процесса `"orchestrate"`). |
| `en` | Английская — загружается для обратной совместимости. Функционально эквивалентна `*`. Новый английский контент должен добавляться в `*`. |
| `ko`, `ja`, `zh`, `es`, `fr`, `de`, `pt`, `ru`, `nl`, `pl` | Языко-специфичные — загружаются только когда в `.agents/oma-config.yaml` задано `language: <lang>`. |

**Следствие**: если задать `language: en` в `.agents/oma-config.yaml`, загружаются только паттерны `*` и `en`. Корейские/японские и т. д. естественно-языковые триггеры не сработают, даже если пользователь пишет на этих языках. Чтобы включить не-английский язык, задайте `language: <code>` соответственно. Английский фоллбэк в `*` всегда остаётся активным.

### Поле Pattern (сырой regex) {#pattern-field-raw-regex}

В дополнение к литеральным `keywords` каждый рабочий процесс может объявить `patterns` — сырые regex-строки, компилируемые с флагами `iu`. Паттерны позволяют сопоставлять многотокеновое намерение, что иначе потребовало бы комбинаторных списков ключевых слов.

```jsonc
{
  "workflows": {
    "orchestrate": {
      "persistent": true,
      "keywords": { "*": ["orchestrate"], "en": ["parallel", ...] },
      "patterns": {
        "*": ["\\b(build|create|make)\\s+(?:an?|the)\\s+...\\b"],
        "ko": ["(앱|API|...)\\s*(?:을|를)?\\s*(?:만들어\\s*(?:주세요|줘)?|...)"]
      }
    }
  }
}
```

Правила оформления:
- Строки компилируются напрямую — экранируйте обратные слэши дважды: один раз для JSON, один раз для regex (`\\b`, `\\s+`)
- Автоматического оборачивания границей слова нет — авторы паттернов сами расставляют `\b`
- Невалидный regex молча пропускается во время выполнения (заметно во время правки конфига через падение тестов)

### Фильтрация информационных паттернов

Секция `informationalPatterns` в `.agents/hooks/core/triggers.json` определяет фразы, обозначающие вопросы, а не команды. Проверка выполняется в окне 60 символов вокруг каждого потенциального совпадения с рабочим процессом:

| Секция | Примеры паттернов |
|--------|------------------|
| `*` (универсальный английский) | "what is", "what are", "how to", "how does", "how do", "should we", "should i", "could we", "would you", "what if", "what about", "why build", "false positive", "trigger when", "auto-trigger" |
| `ko` | "뭐야", "무엇", "어떻게", "설명해", "알려줘", "트리거", "발동", "메타", "왜 만들", "어떻게 만들", "어떨까", "한다면", "할까요" |
| `ja` | "とは", "って何", "どうやって", "説明して" |
| `zh` | "是什么", "什么是", "怎么", "解释" |

Если ввод соответствует и триггеру рабочего процесса, и информационному паттерну, приоритет у информационного — рабочий процесс не запускается. Именно это блокирует промпты вроде:
- `"How do you build a TODO app?"` — `how do` в `*` блокирует regex-намерение orchestrate
- `"orchestrate 트리거 해주면 되나요?"` (при `language: ko`) — `트리거` в `ko` блокирует ключевое слово orchestrate

### Исключённые рабочие процессы

Требуют явной `/command`: `/scm`, `/tools`, `/stack-set`, `/exec-plan`, `/convert`.

---

## Механика постоянного режима

### Файлы состояния

```
.agents/state/
├── orchestrate-state.json
├── ultrawork-state.json
└── work-state.json
```

Содержат: имя рабочего процесса, текущую фазу/шаг, ID сессии, временную метку и незавершённое состояние.

### Подкрепление

Хук `persistent-mode.ts` внедряет `[OMA PERSISTENT MODE: {name}]` в каждое сообщение.

### Деактивация

Для деактивации скажите «workflow done» (или эквивалент на настроенном языке). Это удаляет файл состояния из `.agents/state/`, прекращает внедрение контекста постоянного режима и возвращает к обычной работе.

Рабочий процесс также может завершиться естественным образом при выполнении всех шагов и прохождении финального шлюза.

---

## Типичные последовательности

### Фича в одном домене
```
Describe the task → relevant skill → implement → focused verification
```

### Сложный проект
```
/work → PM plans → review within authorized scope → agents spawn → QA reviews → fix issues → report
```

### Автоматическая параллельная реализация
```
/orchestrate → load or create plan → resolve dependencies → spawn independent tasks → verify → report
```

### Максимальное качество
```
/ultrawork -> PLAN (4 ревью) -> IMPL -> VERIFY (3 ревью) -> REFINE (5 ревью) -> SHIP (4 ревью)
```

### Отладка
```
/debug -> воспроизведение -> корневая причина -> исправление -> регрессионный тест -> сканирование
```

### От дизайна к реализации
```
/brainstorm -> документ -> /plan -> задачи -> /orchestrate -> параллельная реализация -> /review -> /scm
```

### Настройка новой кодовой базы
```
/deepinit -> AGENTS.md + ARCHITECTURE.md + docs/
```

### Повторное выполнение с независимой проверкой
```
/ralph → define criteria → ultrawork → judge → repeat as needed → completion, partial completion, or safeguard report
```
